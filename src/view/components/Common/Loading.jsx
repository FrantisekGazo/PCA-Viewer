"use strict";

const React = require('react');
const CircularProgress = require('material-ui/CircularProgress').default;


/**
 * Simple component that shows progress bar.
 */
module.exports = ({}) => (<CircularProgress/>);
