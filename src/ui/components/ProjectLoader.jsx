"use strict";

const React = require('react');


const ProjectLoader = ({}) => {
    return (
        <div>
            <h2>Loader</h2>
        </div>
    )
};

ProjectLoader.propTypes = {
};

module.exports = ProjectLoader;
