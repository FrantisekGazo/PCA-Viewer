"use strict";

const React = require('react');


const ProjectContent = ({}) => {
    return (
        <div id="content">
            Content
        </div>
    );
};

ProjectContent.propTypes = {
};

module.exports = ProjectContent;
